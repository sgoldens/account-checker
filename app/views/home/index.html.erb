<% if current_user %>
  <div class="panel panel-default">
    <div class="panel-body">
      <p><%= link_to "Search Reddit.com", new_search_url %></p>
    </div>
  </div>
<% else %>
  <%= render 'welcome' %>
<% end %>