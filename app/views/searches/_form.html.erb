<%= form_for(Search.new(user: current_user)) do |f| %>
    <%= f.label "User name search", content: "User name", class: "col-sm-2" %>
    <%= f.text_field :term, id: 'user', placeholder: "Type a name..." %>
  <%=f.submit "Search", class: "btn btn-primary"%>
<% end %>